package Lessons;

public class Java22_Constructor
{
    /*
    Constructor
    -are very special methods using creating object of the class
    -every class must have a constructor, if you do not create a constructor compiler give a default (no-argument) one
    -constructor are the methods which has the class name
    -it can not have return type of specifier only calls name and parameter
    -only a constructor can call another constructor
    - execution depends on the creation of the object
    -creating an object is
    Classname referenceName = new existingConstructor
    -if you want more than one you need to overload
     */
}
